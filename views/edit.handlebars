{{!-- {{> menu }}

<form id="edit-post-form" data-id="{{id}}">
  <input type="text" name="title" value="{{title}}" required />
  <textarea name="content" required>{{content}}</textarea>
  <button type="submit">Update Post</button>
</form>
<script src="/js/edit.js"></script>  --}}

{{> menu }}

<form id="edit-post-form" data-id="{{id}}" class="mb-4">
  <div class="form-group">
    <input type="text" name="title" value="{{title}}" required class="form-control" />
  </div>
  <div class="form-group">
    <textarea name="content" required class="form-control">{{content}}</textarea>
  </div>
  <button type="submit" class="btn btn-primary">Update Post</button>
</form>

<script src="/js/edit.js"></script>
